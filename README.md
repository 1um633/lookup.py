I am lazy. There is no help menu but usage is simple and straight forward.

Before installing requests will need to be installed in termux.

pip install requests

Usage:

python lookup.py username

Change username to target twitter handle.

Good Luck!
